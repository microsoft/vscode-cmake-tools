name: Create digest
on:
  workflow_dispatch:
  pull_request:

jobs:
  main:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Actions
        uses: actions/checkout@v2
      - name: Create digest
        uses: ./.github/actions/digest
